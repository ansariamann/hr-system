{
    "name": "hr-system-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "cd frontend && npm run dev",
        "build": "cd frontend && npm run build",
        "start": "cd frontend && npm run start",
        "api": "uvicorn ats_backend.main:app --reload --host 0.0.0.0 --port 8000",
        "install:frontend": "cd frontend && npm install"
    }
}